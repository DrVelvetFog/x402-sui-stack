{
  "name": "x402-sui-stack",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The x402 builder stack for Sui — a non-custodial payments facilitator, developer tooling (x402-pilot + conformance MCP), and a runnable golden-path demo that settles a paid API call on Sui.",
  "license": "Apache-2.0",
  "scripts": {
    "demo": "tsx examples/quickstart/demo.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mysten/sui": "^2.17.0",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
